# 프로젝트 메모리 (진행 기록 · 결정사항)

`CLAUDE.md`가 "작업할 때 알아야 할 것"이라면, 이 파일은 "왜 이렇게 됐는지"에 대한 기록입니다. 나중에 이 프로젝트를 다시 열었을 때 맥락을 빠르게 복원하기 위한 용도입니다.

## 원래 요청 (2026-08-24 시작)
사용자는 "널위한문화예술"과 "아츠앤트래블" 두 유튜브 채널의 영상/쇼츠 설명란에 있는 전시 정보를 자동으로 모아, 별도의 전시정보 캘린더로 보고 싶어했습니다. 핵심 요구사항:
- 컴퓨터 전원 상태와 무관하게 자동 업데이트되어야 함 → 로컬 스크립트가 아니라 **클라우드 예약 실행**이 필요하다는 뜻
- 캘린더에 전시제목(장소/건국대병원 기준 대중교통 소요시간/야간개장 요일·시간)을 표시
- 상세 화면에는 작가·장소및주소·전시시간·전시특징·작가소개·추천 영상 링크

## 주요 결정들과 이유
- **정적 웹앱(바닐라 JS) + Vercel**, Next.js 같은 프레임워크는 안 씀 — 빌드 과정 없이 단순하게 유지하고, 클라우드 자동화 에이전트가 데이터 파일만 고치면 되도록 하기 위함
- **Google 캘린더 연동 아님** — 사용자가 명시적으로 "구글캘린더 연동이 아니라 별도 앱"을 원함
- **디자인은 두 단계**: 1차로 직접 만든 월간 그리드 디자인 → 2차로 사용자가 별도로(다른 Claude 세션/Claude Design으로 추정) 만들어온 4탭(캘린더/지도/저장/더보기) 고정밀 디자인 handoff 문서를 받아서 그대로 구현으로 전환. `클로드 디자인/` 폴더의 handoff 문서가 그 기준임
- **전시별 색상 코드**는 handoff 문서에 지정된 7색을 그대로 사용 (`data/exhibitions.json`의 `color`/`colorSoft` 필드)
- **지도**: 처음엔 추상 배경 + 수동 좌표 배치로 시작 → 사용자가 실제 지도를 원해서 네이버 지도(Naver Maps) NCP Application을 새로 만들고 실제 SDK로 교체함
- **자동 업데이트 주기는 매주 1회**(처음엔 매일 검토했다가 사용자가 "영상이 그렇게 자주 안 올라온다"며 주 1회로 확정), 매주 월요일 오전 9시(KST)
- **알림은 진짜 푸시가 아니라 인앱 배너** — 정적 사이트라 실제 백그라운드 알림은 서버/네이티브 앱이 필요해서 한계를 명확히 안내함

## 겪었던 이슈와 해결
- Claude 계정을 세션 중간에 전환하면서 이전에 만든 예약 루틴용 `environment_id`가 무효화됨 → `/schedule` 스킬을 다시 불러 새 environment_id로 재생성해서 해결
- 네이버 지도 스크립트 파라미터가 `ncpClientId`인 줄 알았는데 실제로는 `ncpKeyId`로 바뀌어 있어서 처음엔 "인증 실패" 에러가 났음 (공식 문서 재확인 후 수정)
- claude.ai 루틴이 GitHub 저장소에 접근하려면 **claude.ai 자체에 GitHub 계정을 연동**해야 함(로컬 git push 인증과는 완전히 별개) — `claude.ai/customize/connectors`에서 처리
- 상세시트 대표 이미지가 유튜브 썸네일 비율(16:9)과 이미지 박스 비율(약 3:1)이 안 맞아 텍스트가 잘리는 문제 → `object-fit: cover`를 `contain`으로 바꿔서 해결(작은 목록 썸네일은 `cover` 유지)
- Claude 아티팩트 미리보기는 외부 이미지 로딩을 막는 보안정책이 있어서, 미리보기에서는 썸네일이 색상 블록으로만 보임(실제 배포 사이트에서는 정상) — 실제 버그와 헷갈리지 않도록 구분해서 설명함
- 앱 아이콘을 처음엔 Claude가 직접 SVG로 디자인해서 만듦(캘린더+액자 모티프) → 이후 사용자가 디자인팀이 만든 진짜 아이콘 세트(`전시정보 캘린더 앱 디자인/app_icon/`, 핑크·보라 패널 모티프)를 폴더에 넣어주면서 그걸로 교체함. `icons/`, `앱아이콘.png/.ico`는 전부 그 원본에서 리사이즈한 결과물임
- 이 프로젝트 폴더 자체에 Windows 탐색기 커스텀 폴더 아이콘을 지정함(`desktop.ini` + `앱아이콘.ico`, 폴더 System 속성 필요) — 사용자가 "이 폴더 아이콘으로 지정해줘"라고 요청해서 추가한 기능, 앱 동작과는 무관한 로컬 편의 기능임

## iOS 홈 화면 웹앱(PWA) 위치 권한 안 되던 문제 — 최종 해결 (2026-08-25)
증상: Safari/크롬 탭에서 사이트를 열면 위치 권한 팝업이 정상적으로 뜨고 허용하면 잘 작동하는데, **"홈 화면에 추가"한 아이콘으로 열면 권한 팝업 자체가 안 뜨고 조용히 실패**함(설정 > 위치 서비스 목록에도 그 앱이 아예 안 나타남).

시도했지만 원인이 아니었던 것들: 전체 위치 서비스 토글, 스크린타임 제한(애초에 꺼져있었음), Safari 사이트별 위치 설정("허용"으로 이미 되어 있었음), `apple-mobile-web-app-capable` 메타태그 추가, manifest에 `scope`/`id` 필드 추가, 홈 화면 아이콘 삭제 후 재설치(여러 번). 전부 정상적인 개선이긴 하지만 이 특정 증상의 직접적인 원인은 아니었음.

**진짜 원인 및 해결법**: iOS 기기 내부의 권한 기록 데이터베이스 자체가 꼬여있던 것으로 추정됨(웹 개발/앱 코드 문제가 아니라 기기 상태 문제). 애플 공식 지원팀 채팅 문의로 해결:
1. 설정 > 일반 > 아이폰(iPad) 전송 또는 재설정 > 재설정 > **"위치 및 개인정보 보호 재설정"**
2. 아이폰 재시동
3. 홈 화면 앱을 다시 열어서 위치 기능 사용 → 이번엔 정상적으로 권한 요청 팝업이 뜨고 허용하니 작동함

**애플 상담원 추가 권장사항** (재발 방지용 사전 점검 체크리스트):
- 설정 > 개인정보 보호 및 보안 > 위치 서비스 > **시스템 서비스** — "위치"/"지도"/"거리" 관련 키워드 옵션들은 다 켜둘 것
- 설정 > 개인정보 보호 및 보안 > **차단 모드(Lockdown Mode)** — 켜져 있으면 위치 공유가 안 될 수 있어 꺼둘 것
- **저전력 모드**가 켜져 있으면 전력 절약을 위해 위치 기능이 제한될 수 있음

**중요**: 이건 코드를 아무리 고쳐도 해결 안 되는, 그 기기에 국한된 문제였을 가능성이 높음. 앞으로 다른 지도 연동 앱(네이버지도 등)을 새로 만들 때 홈 화면 앱에서 위치 권한이 똑같이 막히면, 코드/설정을 더 뒤지기 전에 **이 재설정부터 먼저 시도**할 것. (다만 매번 재설정하기보다는, 애초에 `apple-mobile-web-app-capable` 메타태그와 manifest의 `scope`/`id`는 처음부터 넣어두는 게 좋은 습관 — 원인은 아니었어도 표준적으로 필요한 설정임)

## 1차 완료 이후 추가 기능/수정 (2026-08-25, 같은 날)
1차로 "완료"라고 확인받은 뒤, 실제로 써보시면서 아래를 추가 요청하셔서 반영함:
- **입장료(admission) 필드 추가** — 상세시트 2단 그리드 아래에 전체너비로 표시, 정보 없으면 자동으로 숨김. 자동화 루틴도 앞으로 입장료를 같이 조사하도록 `AGENT_PROMPT.md`에 반영
- **지도 핀/캐러셀 카드를 탭하면 바로 상세 바텀시트가 열리도록 개선** — 원래는 포커스/캐러셀 스크롤만 되고 상세보기는 따로 안 열렸음
- **"내 위치" 버튼 추가** — 지도 탭에 📍 버튼, 브라우저 위치 허용 시 직선거리 기반 대략적 이동시간을 상세시트/캐러셀 카드에 추가 표시(건대병원 기준 값은 그대로 유지, 실제 대중교통 경로 계산 API는 아직 연동 안 함)
- **바젤리츠 전시 시작일 정정**(07-01 추정 → 실제 08-13으로 리서치 확인)
- **최근 3주치 영상 백필** — 처음엔 최근 1주일치만 등록했는데, 이미 끝난 전시는 빼고 3주치로 확장 요청받아 14건 추가(총 7건 → 21건). 채널2(아츠앤트래블)의 "8월 서울 전시 캘린더" 요약 영상 하나에 여러 전시가 한꺼번에 소개되어 있어서 그걸 활용해 다수 확보함
- **네이버지도 길찾기 링크 버그 수정** — "장소명+전체주소"를 합쳐서 검색했더니 검색결과가 안 나오는 문제 발견 → 장소명만 검색하도록 수정 (자세한 내용은 `CLAUDE.md` 참고)
- **iOS 홈 화면 앱 위치 권한 문제 최종 해결** — 아래 별도 섹션 참고

## 완료 확인 (2026-08-25)
사용자가 배포 사이트·지도·아이콘·문서 전부 확인 완료. 앱 아이콘은 디자인팀 제공 버전(핑크/보라 패널)으로 최종 확정됨. 이후 위 "1차 완료 이후 추가 기능/수정"까지 전부 반영 후 최종 확인 완료.

이 프로젝트와 같은 구조를 재사용해서 "맛집 캘린더" 앱을 다음으로 만들기로 함(이번 주말 착수 예정) — 별도 폴더 `../[앱]맛집정보 캘린더/아이디어_노트.md`에 계획 정리해둠.

## 남은 선택지 / 나중에 할 수도 있는 것
- **유튜브 커뮤니티 게시글(포스트)의 전시 정보는 자동으로 못 가져옴** — RSS/공식 API가 없고, 페이지도 JS 렌더링이라 WebFetch로는 내용이 안 보임(2026-08-26 확인). 대안으로 "더보기 탭에 텍스트/URL 붙여넣으면 자동 등록되는 기능"을 구상 중 — Vercel 서버리스 함수 등 새 인프라가 필요해서, 맛집 캘린더 앱([[matjip-calendar-app-idea]])에서 먼저 제대로 설계한 뒤 이 앱에도 이식하기로 함(2026-08-26 결정). 그 전까지는 커뮤니티 게시글 내용을 발견하면 그냥 채팅으로 링크/내용을 알려주면 수동으로 등록 가능
- 저장(북마크) 종료 임박 알림을 진짜 푸시 알림으로 바꾸려면 서버나 네이티브 앱이 필요함 (현재는 인앱 배너로 타협)
- 지도 마커가 실제 거리 기준으로는 서로 가까워서 겹칠 수 있음(예: 세화미술관·국립현대미술관 서울관) — 사용자가 직접 확대하면 분리됨, 별도 클러스터링 로직은 아직 없음
- 전시 이미지는 유튜브 썸네일 자동 추출이 기본값이고, 품질이 아쉬우면 `data/exhibitions.json`에 `imageUrl` 필드를 수동으로 추가해서 덮어쓸 수 있음
